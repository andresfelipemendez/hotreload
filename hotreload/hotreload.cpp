// hotreload.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>

int main()
{
    std::cout << "Hello World!\n";

    // if dll doesn't exist exit
    
    // hardcoding path for now
    std::string originalDLL = "C:\\Users\\andres\\Development\\hotreload\\x64\\Debug\\enginelib.dll";

    // if copy doesn't exist create one

    bool running = true;
    while (running) {

    }
}
